---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time class="blogtime" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
